# cloudcover-aws-sdk-go-v2

Provides data on the aws-sdk-go-v2 for CloudCover - static links to a Go library in `generator` which uses `golang.org/x/tools/go` to analyse the SDK's source code. The crate returns a mapping of Go method references within the SDK to the API methods they expose.
