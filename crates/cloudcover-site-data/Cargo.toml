[package]
name = "cloudcover-site-data"
version.workspace = true
edition.workspace = true

[[bin]]
name = "cloudcover-site-data"
path = "src/main.rs"

[dependencies]
cloudcover-aws = { path = "../clouds/cloudcover-aws" }
cloudcover-core = { path = "../cloudcover-core" }
cloudcover-terraform-provider-aws = { path = "../sdks/cloudcover-terraform-provider-aws" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3.0"

[lints]
workspace = true
