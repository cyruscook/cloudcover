[package]
name = "cloudcover-site-data"
version.workspace = true
edition.workspace = true
description = "Static site data generator for CloudCover"
license.workspace = true
repository.workspace = true

[[bin]]
name = "cloudcover-site-data"
path = "src/main.rs"

[dependencies]
cloudcover-aws = { version = "0.1.0", path = "../clouds/cloudcover-aws" }
cloudcover-core = { version = "0.1.0", path = "../cloudcover-core" }
cloudcover-terraform-provider-aws = { version = "0.1.0", path = "../sdks/cloudcover-terraform-provider-aws" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3.0"

[lints]
workspace = true
